{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-save-storage",
  "description": "Allows only the bundled main window to use Aethoria's slot-based save storage commands.",
  "local": true,
  "windows": ["main"],
  "platforms": ["linux", "macOS", "windows"],
  "permissions": ["allow-save-storage"]
}
